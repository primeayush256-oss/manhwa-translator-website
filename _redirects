# Cloudflare Pages / Workers Static Assets redirects file.
# Cloudflare automatically serves checkout.html at /checkout via built-in Clean URLs handling.
# Do NOT add "/checkout /checkout.html 200" here as that causes a 307 infinite redirect loop with Clean URLs.
